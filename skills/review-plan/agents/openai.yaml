interface:
  display_name: "计划审查"
  short_description: "Research-backed plan review and execution tracking"
  default_prompt: "Use $review-plan to review this development plan against the current project, research relevant context, and ask me before editing or splitting it."

dependencies:
  tools:
    - type: "mcp"
      value: "exa"
      description: "Exa MCP server for current web and code research"
